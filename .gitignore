# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# NOTE: pubspec.lock is intentionally NOT ignored — this is an application, and
# committing the lockfile pins transitive dependency versions for reproducible
# builds. Commit it after the first `flutter pub get`.

# Android
android/local.properties
android/**/build/
android/.gradle/
*.jks
android/key.properties

# IDE
.idea/
*.iml
.vscode/
