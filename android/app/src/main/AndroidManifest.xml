<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
      Still no INTERNET permission in the release manifest — see the note in
      src/debug/AndroidManifest.xml about why the DEBUG build differs, and
      DEPLOYMENT.md for how to verify the shipped release APK/AAB has none.
    -->

    <application
        android:name=".VaultKeyApplication"
        android:label="VaultKey"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          The IME's Kotlin shell (FlutterVaultIME) lives in THIS module
          rather than android/keyboard, specifically so it can resolve the
          Flutter engine dependency the same way MainActivity does — see
          INTEGRATION.md for why splitting that across modules would be
          fragile. android/keyboard now only supplies the pure-logic
          CredentialSuggestionInjector this service calls into.
        -->
        <service
            android:name=".FlutterVaultIME"
            android:label="VaultKey Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- The autofill <service> entry still comes in automatically via
             manifest merging from the :autofill module dependency. -->
    </application>
</manifest>
