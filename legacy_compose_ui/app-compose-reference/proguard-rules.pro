# Room/SQLCipher and Compose already ship consumer proguard rules for
# themselves. Add app-specific keep rules here only if R8 ends up stripping
# something reflection-based (e.g. if you later add a JSON export feature).
